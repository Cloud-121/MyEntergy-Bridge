# Selenium Standalone Firefox Container (selenium/Dockerfile)
FROM selenium/standalone-firefox:latest

# Expose the port for Selenium WebDriver (default 4444)
EXPOSE 4444

# No additional services needed, since we are using the standard selenium image
